<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-09-17T16:45:00Z" />
  <pre_design_run>
    <total>664</total>
    <passed>568</passed>
    <failed>1</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
      <failure test="test_status_reflects_any_high_severity_warning_not_just_bids_errors" file="tests/test_cli_integration.py" line="326">
        <error_type>TypeError</error_type>
        <message>test_status_reflects_any_high_severity_warning_not_just_bids_errors.&lt;locals&gt;._classify_with_injected_warning() got an unexpected keyword argument 'scout_keywords'</message>
        <traceback>fmri_bids_recon/pipeline.py:189: TypeError: the monkeypatched classify() replacement did not accept the keyword arguments pipeline.py's Phase 1 call site now passes.</traceback>
      </failure>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
    <disposition test="test_status_reflects_any_high_severity_warning_not_just_bids_errors" file="tests/test_cli_integration.py" classification="obsolete-test">
      <intended_contract>BidsReconResult.status reflects any HIGH-severity warning present in the graded_warning accumulator, not only BIDS_VALIDATION_ERRORS. Verified by monkeypatching pipeline_mod.classify with a wrapper that calls the real classify() and then injects a synthetic warning via graded_warning().</intended_contract>
      <current_test_claim>The wrapper is defined as _classify_with_injected_warning(all_series) -- a single positional parameter, forwarding only all_series to the real classify().</current_test_claim>
      <evidence>pipeline.py's Phase 1 call site (line 189) now calls classify(all_series, scout_keywords=..., calibration_keywords=..., norm_tokens=..., expected_anat_count=...) following the config-schema work applied earlier in this session. The wrapper's signature predates that change and has no way to accept the new keyword arguments, producing a TypeError unrelated to the status-gating behavior under test.</evidence>
      <action>Re-expressed the wrapper's signature only: _classify_with_injected_warning(all_series, **kwargs), forwarding **kwargs to the real classify(). No assertion was touched; the postcondition (status == "warning", exactly one TEST_INJECTED_WARNING, zero bids_validation_errors) is unchanged.</action>
    </disposition>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>47</tests_created>
    <tests_modified>1</tests_modified>
    <files_created>
      <file path="tests/test_config.py" test_count="18" coverage_target="7 new config fields (scout_keywords, calibration_keywords, norm_tokens, expected_anat_count, registry_mode, label_freeze_mode, rename_detection): default values, YAML overrides, enum validation, and expected_anat_count type/case-normalization validation." />
      <file path="tests/test_classify.py" test_count="21" coverage_target="Cross-vendor scout keyword defaults (AAScout, AAHScout, Locator, Scanogram, Plan Scan, PlanScan, Topogram) and parameter-replaces-default semantics; cross-vendor calibration keywords (_cal_, calibration, coilsurv); norm_tokens vendor-agnostic recognition (GE PURE, Philips CLEAR) and parameter-replaces-default semantics; the _is_spin_echo PSD token-boundary fix (no false positive on _sense/sensePA); the two-key anatomical calibration gate (expected_anat_count) across its branches." />
      <file path="tests/test_labels.py" test_count="4" coverage_target="label_freeze_mode=re-derive (drift advisory instead of halt, and the no-drift case); rename_detection=warn (advisory instead of halt) and rename_detection=off (check skipped entirely)." />
      <file path="tests/test_runs.py" test_count="1" coverage_target="registry_mode=advisory retains a volume-mismatched known series with a HIGH-severity, user-facing warning instead of excluding it." />
      <file path="tests/test_assemble.py" test_count="1" coverage_target="Regression test for the excluded-BOLD-still-in-roles crash: assemble() must skip an excluded series via the excluded_sns guard rather than raising KeyError on run_indices[snum]." />
      <file path="tests/test_cli_integration.py" test_count="1" coverage_target="Integration test confirming expected_anat_count set in the study YAML flows through pipeline.py's Phase 1 wiring into classify(), producing exactly one T1w file in a real end-to-end run." />
    </files_created>
    <design_rationale>
      Coverage was scoped directly to the ten changes applied in the prior implement:build pass (7 new config fields; the excluded-series crash fix; vendor-agnostic norm-token detection; the spin-echo PSD substring fix; the two-key anatomical calibration gate; expanded cross-vendor scout/calibration keyword vocabularies; label-freeze and rename-detection advisory modes; registry-mode advisory volume enforcement; and the pipeline wiring that connects all of the above). Each new function-level parameter was tested both for its non-default behavior and, where not already covered by a pre-existing test exercising the omitted-parameter path, its backward-compatible default. Deliberately not duplicated: backward-compatibility pins for behavior already exercised, unchanged, by pre-existing tests that passed in the pre-design run (e.g., the original DUPLICATE_MODALITY MEDIUM-severity path, the frozen-label-drift halt, the strict rename-collision halt, and the strict registry-mode exclusion) -- re-testing these would have been redundant with tests already proven correct against the new code.

      One coverage item (the assemble()-level regression test for the excluded-series crash) was identified during design analysis but omitted from the initial consolidated approval message; it was surfaced to the user as an explicit gap rather than silently added or silently dropped, and added only after explicit approval.
    </design_rationale>
  </design_phase>
  <post_design_run>
    <total>711</total>
    <passed>615</passed>
    <failed>1</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
      <failure test="test_expected_anat_count_config_field_flows_through_to_a_real_pipeline_run" file="tests/test_cli_integration.py" line="401">
        <error_type>AssertionError</error_type>
        <message>assert 3 == 0</message>
        <traceback>tests/test_cli_integration.py:401: assert exit_code == 0</traceback>
        <likely_cause>Test-authoring defect in this newly-created test, not a product defect. The two-key anatomical calibration gate's demotion path legitimately emits a HIGH-severity ANAT_CALIBRATION_DEMOTION graded_warning by design (this is the correct, intended behavior for a demotion event that should be surfaced for review). Per the pipeline's pre-existing exit-code semantics (see test_a_high_severity_warning_without_bids_errors_still_exits_three), any HIGH-severity graded_warning with no BIDS validation errors maps to exit code 3, not 0. The test's expectation of exit_code == 0 was simply wrong. Corrected immediately after this run to expect exit_code == 3, with an added comment citing the exit-code precedent; the corrected assertion was confirmed to pass via a direct, isolated re-run of only this one test (not a full formal suite re-dispatch, per this skill's single-pass discipline against self-initiated re-loops). A subsequent /test invocation will show this test passing in a fully re-verified suite run.</likely_cause>
      </failure>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items>
  </action_items>
</test_report>
