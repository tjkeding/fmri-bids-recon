<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-07-28T17:08:00-04:00" />

  <pre_design_run>
    <total>0</total>
    <passed>0</passed>
    <failed>0</failed>
    <errors>4</errors>
    <coverage_pct />
    <notes>
      pytest aborts entirely on collection errors by default, so the recorded run shows 0/0/0.
      An independent --collect-only pass (run by the orchestrator directly, not the dispatched
      agent) put the true baseline at 357 collectible tests across the 18 unaffected files, with
      4 files blocked at import time: test_cli_integration.py, test_convert.py,
      test_json_intermediate.py, test_physio.py (all ImportError, referencing names retired by
      the preceding /implement build: associate_physio, index_source_dicoms, AcquisitionInfo,
      _parse_acquisition_info_block). A supplementary --continue-on-collection-errors run
      (orchestrator-side, for design-phase evidence gathering only) additionally surfaced 19
      failures hidden behind those 4 collection errors: 13 in test_sidecar.py (load_series()
      unpacking, since its return type changed) and 6 in test_deface.py (5 ImportError on the
      renamed _ensure_fsl_env, 1 AssertionError from a test whose assertion checked the exact
      os.environ global-mutation bug the build deliberately fixed).
    </notes>
    <failures>
      <failure test="collection: test_cli_integration.py" file="tests/test_cli_integration.py" line="29">
        <error_type>ImportError</error_type>
        <message>cannot import name 'associate_physio' from 'fmri_bids_recon.physio'</message>
      </failure>
      <failure test="collection: test_convert.py" file="tests/test_convert.py" line="29">
        <error_type>ImportError</error_type>
        <message>cannot import name 'index_source_dicoms' from 'fmri_bids_recon.stage1_convert'</message>
      </failure>
      <failure test="collection: test_json_intermediate.py" file="tests/test_json_intermediate.py" line="27">
        <error_type>ImportError</error_type>
        <message>cannot import name 'AcquisitionInfo' from 'fmri_bids_recon.physio'</message>
      </failure>
      <failure test="collection: test_physio.py" file="tests/test_physio.py" line="36">
        <error_type>ImportError</error_type>
        <message>cannot import name '_parse_acquisition_info_block' from 'fmri_bids_recon.physio'</message>
      </failure>
    </failures>
  </pre_design_run>

  <failing_test_dispositions>
    <disposition test="test_sidecar.py (13 tests, all load_series() call sites)" file="tests/test_sidecar.py" classification="obsolete-test">
      <intended_contract>load_series() returns the imaging Series objects found in a staging directory.</intended_contract>
      <current_test_claim>Unpacks load_series()'s return directly as the series list (e.g. `(s,) = load_series(...)`), or compares it to `[]`.</current_test_claim>
      <evidence>fmri_bids_recon/sidecar.py load_series() now returns tuple[list[Series], list[Path]] per this build's change C1, to separate imaging series from physio sidecars.</evidence>
      <action>Re-expressed: every call site unpacks the new tuple (`(s,), _ = load_series(...)`; `load_series(...)[0]`; `== ([], [])`). Postcondition on the series list itself is unchanged and unweakened.</action>
    </disposition>
    <disposition test="test_deface.py (5 tests, direct _ensure_fsl_env calls)" file="tests/test_deface.py" classification="obsolete-test">
      <intended_contract>FSL's bin directory is made reachable for pydeface's internal subprocess calls, and FSLOUTPUTTYPE is set if unset.</intended_contract>
      <current_test_claim>Calls the retired _ensure_fsl_env() and asserts against os.environ directly.</current_test_claim>
      <evidence>fmri_bids_recon/deface.py's _ensure_fsl_env() was replaced by _build_fsl_env(), which returns a scoped dict instead of mutating os.environ (this build's change C5).</evidence>
      <action>Re-expressed against _build_fsl_env()'s returned dict. Strengthened, not just re-pointed: each test now also asserts os.environ was NOT mutated, since that is exactly the property the fix introduced.</action>
    </disposition>
    <disposition test="test_deface_amends_path_via_ensure_fsl_env_before_invoking_the_tool" file="tests/test_deface.py" classification="obsolete-test">
      <intended_contract>deface() makes FSL's bin directory reachable to the defacing subprocess before invoking it.</intended_contract>
      <current_test_claim>Asserts fsl_bin appears in os.environ["PATH"] after calling deface().</current_test_claim>
      <evidence>Same as above; this assertion's failure (fsl_bin not in os.environ["PATH"]) is the fix working as intended, not a regression.</evidence>
      <action>Re-expressed as an integration test verifying the subprocess.run() call receives an explicit env= kwarg containing fsl_bin on PATH, while os.environ itself remains untouched. Mechanism-level intent (FSL reachable to the subprocess) is preserved; the scoping property is added.</action>
    </disposition>
    <disposition test="7 tests in test_convert.py (index_source_dicoms section)" file="tests/test_convert.py" classification="obsolete-test">
      <intended_contract>index_source_dicoms() groups source DICOM files by SeriesNumber, the sole channel through which PhysioLog series reached the engine.</intended_contract>
      <current_test_claim>Directly exercises index_source_dicoms() and its DicomSeriesRecord output.</current_test_claim>
      <evidence>index_source_dicoms(), DicomSeriesRecord, and StagingResult.dicom_index were removed entirely in this build's change C3 -- confirmed via repo-wide grep to have no remaining callers once physio.py stopped depending on them.</evidence>
      <action>Removed, not re-expressed: the function has no replacement or analog (physio now reaches the engine via dcm2niix's native staging output, tested separately in test_physio.py). The 9 dcm2niix-argv tests in the same file were untouched (aligned; C3 did not change that contract).</action>
    </disposition>
    <disposition test="4 tests in test_json_intermediate.py (physio dataclass round-trips)" file="tests/test_json_intermediate.py" classification="obsolete-test">
      <intended_contract>The generic dataclass encoder/decoder round-trips a class with nested dataclasses and Optional fields.</intended_contract>
      <current_test_claim>Round-trips PhysioLog/PhysioChannel/AcquisitionInfo specifically.</current_test_claim>
      <evidence>These three classes were removed from physio.py in this build's change C2; json_intermediate.py's _DATACLASS_TYPES no longer registers them (change C6).</evidence>
      <action>Removed, not re-expressed: the same coverage shape (dict-of-nested-dataclasses, Optional-nested-dataclass) is already independently exercised by test_registry_delta_round_trips_with_a_dict_of_nested_dataclasses and test_series_with_none_optional_fields_round_trips. No coverage is lost.</action>
    </disposition>
    <disposition test="imports, STAGE_FUNCTIONS, session fixture in test_cli_integration.py" file="tests/test_cli_integration.py" classification="obsolete-test">
      <intended_contract>__main__'s call sites bind against the real signatures of the stage functions it imports (an AST-level check that a call cannot silently fail to bind).</intended_contract>
      <current_test_claim>STAGE_FUNCTIONS maps parse_physio_dicom/associate_physio, both retired; the session fixture's StagingResult stub sets dicom_index, a field that no longer exists.</current_test_claim>
      <evidence>__main__.py's imports and physio call sites were rewired in this build's change C4 to discover_native_physio/associate_native_physio.</evidence>
      <action>Re-expressed: STAGE_FUNCTIONS updated to the new names; the StagingResult stub's dicom_index kwarg removed. Same bind-check mechanism and intent, pointed at the current signatures. 9 AST bind-check tests and 10 general session-flow tests: aligned once the fixture was fixed.</action>
    </disposition>
    <disposition test="test_a_physio_log_in_the_source_directory_reaches_the_pipeline" file="tests/test_cli_integration.py" classification="obsolete-test">
      <intended_contract>A physio recording present at pipeline input reaches the assembled BIDS output when physio is enabled.</intended_contract>
      <current_test_claim>Writes a raw Siemens PMU DICOM to the source directory and enables physio.</current_test_claim>
      <evidence>Physio now reaches the engine via dcm2niix's native staging output (JSON+tsv.gz), not via a raw source DICOM parsed by the retired index_source_dicoms/parse_physio_dicom path.</evidence>
      <action>Rewritten as test_a_native_physio_export_in_staging_reaches_the_pipeline: stages a synthetic native physio triplet directly in the staging directory and asserts a clean run (exit 0) with physio output files present under func/. This is a stronger assertion than the original (which accepted exit 0 OR 1 as "either outcome exercises the wiring") -- it pins the correct successful outcome rather than accepting ambiguity.</action>
    </disposition>
    <disposition test="all 27 tests in test_physio.py" file="tests/test_physio.py" classification="obsolete-test">
      <intended_contract>Physiological recordings are correctly parsed, associated with the right BOLD run via a geometry guard, and exported in BIDS format.</intended_contract>
      <current_test_claim>Exercises the retired Siemens PMU byte-parser (length-prefixed ASCII blocks, LogVersion EJA_1, trigger-threshold filtering) end to end.</current_test_claim>
      <evidence>The entire parser (parse_physio_dicom, _split_length_prefixed_blocks, _parse_data_block, _parse_acquisition_info_block, and the PhysioChannel/AcquisitionInfo/PhysioLog dataclasses) was retired in this build's change C2, replaced by ingestion of dcm2niix's native per-channel BIDS physio export.</evidence>
      <action>Full rewrite. The retired parser has no analog to re-express against (subject deleted), so all 27 old tests were removed. New coverage (20 tests) was written for _load_native_channel, discover_native_physio, _count_trigger_events, _find_trigger_column, associate_native_physio, and write_physio, preserving the SAME core protections in spirit: association must halt on a volume-count mismatch (test_a_volume_count_disagreement_stops_the_pipeline), ambiguous ordering must resolve deterministically (test_a_recording_binds_to_the_run_it_followed_not_to_the_one_it_preceded), output must be valid, verbatim BIDS physio files. The module's pre-existing scope caveat (no real vendor file in the repo; fixtures are this module's own model, not certified against dcm2niix's real byte layout) is preserved and extended.</action>
    </disposition>
    <disposition test="11 of the 20 new test_physio.py tests, plus test_a_native_physio_export_in_staging_reaches_the_pipeline" file="fmri_bids_recon/physio.py" classification="product-bug">
      <intended_contract>_load_native_channel() parses the channel label as "the token between '_recording-' and '_physio.json'" (the function's own docstring) -- dcm2niix's three real channel labels are cardiac, respiratory, and external_trigger, the last of which contains an underscore.</intended_contract>
      <current_test_claim>New tests construct triplets including an external_trigger channel (matching dcm2niix's real, confirmed-this-session naming) and assert discover_native_physio/associate_native_physio/write_physio succeed against it.</current_test_claim>
      <evidence>fmri_bids_recon/physio.py:81 uses the regex `_recording-([^_]+)_physio\.json$`. `[^_]+` stops at the first underscore, so it never matches any filename containing "recording-external_trigger_physio" -- it only matches single-token labels (cardiac, respiratory). This is confirmed directly: every one of the 11 failures below is the identical PhysioParseError raised at physio.py:83, triggered only on external_trigger-labeled files; the same tests pass for cardiac/respiratory-labeled files.</evidence>
      <action>Routed to /implement (see action_items below). No assertion was edited or weakened to route around this -- the tests correctly encode the function's own documented contract and dcm2niix's real, already-confirmed output format. Left failing on purpose.</action>
    </disposition>
  </failing_test_dispositions>

  <design_phase>
    <tests_created>23</tests_created>
    <tests_modified>19</tests_modified>
    <files_created>
      <file path="tests/test_sidecar.py" test_count="3" coverage_target="Spec-grounded physio sidecar classification (_is_physio_sidecar): a full-field sidecar is excluded from the series list and the NIfTI requirement; a partial one still hits the existing hard-halt." />
      <file path="tests/test_physio.py" test_count="20" coverage_target="dcm2niix-native physio ingestion: per-channel loading, SeriesNumber-based grouping, rising-edge trigger counting (including amplitude-coded pulses), trigger-column discovery (exact match preferred, substring fallback), SeriesNumber-nearest-preceding association with a volume-count geometry guard, and verbatim BIDS export via write_physio." />
      <file path="tests/test_cli_integration.py" test_count="1" coverage_target="End-to-end: a native physio export staged directly (not via source DICOM) reaches func/ through the real pipeline, exit code 0." />
    </files_created>
    <design_rationale>
      Every test file broken by the preceding /implement build was triaged individually rather than
      patched to merely pass collection: test_sidecar.py and test_deface.py needed only re-expression
      (13 + 6 tests) since their underlying contracts were unchanged or deliberately strengthened by
      the build; test_convert.py and test_json_intermediate.py needed deletions with no replacement
      (7 + 4 tests) since their subjects were deleted with no analog and existing coverage already
      protects the same shape elsewhere; test_cli_integration.py needed both (import/fixture
      re-expression plus one full test rewrite, since the old test's premise -- physio arriving via
      raw source DICOM -- no longer holds); test_physio.py needed a full rewrite (27 tests removed, 20
      written) since its entire subject, the custom Siemens PMU parser, was retired wholesale. The new
      test_physio.py coverage is what surfaced the product bug below: writing a fixture that matches
      dcm2niix's real, already-confirmed three-channel naming (rather than only the two single-word
      labels that happen to work) is precisely the kind of adversarial-to-the-implementation case this
      skill exists to construct.
    </design_rationale>
  </design_phase>

  <post_design_run>
    <total>432</total>
    <passed>418</passed>
    <failed>12</failed>
    <errors>0</errors>
    <coverage_pct />
    <notes>Verified via Stage 3 receipt cross-check (nonce, clock, duration, summary, collect-total, receipt-file mtime/nonce/summary): all 8 checks matched, ok=true.</notes>
    <failures>
      <failure test="11 tests in tests/test_physio.py (see disposition ledger above for the full list)" file="tests/test_physio.py" line="">
        <error_type>PhysioParseError</error_type>
        <message>Cannot parse recording label from filename: '{N}_STUDY_..._recording-external_trigger_physio.json'</message>
        <likely_cause>physio.py:81's regex `[^_]+` cannot match a label containing an underscore. Single confirmed root cause across all 11.</likely_cause>
      </failure>
      <failure test="test_a_native_physio_export_in_staging_reaches_the_pipeline" file="tests/test_cli_integration.py" line="294">
        <error_type>PhysioParseError</error_type>
        <message>Cannot parse recording label from filename: '14_STUDY_task_PhysioLog_recording-external_trigger_physio.json'</message>
        <likely_cause>Same root cause, exercised end-to-end: any real physio-enabled run against ABCD/XA30 data would hit this and halt the entire pipeline with exit code 1 (PhysioParseError is a GuardError subclass, re-raised rather than swallowed).</likely_cause>
      </failure>
    </failures>
  </post_design_run>

  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>1</bugs_routed_to_implement>
    <recommendation>implement_fixes</recommendation>
  </summary>

  <action_items>
    <item priority="P0" target_mode="implement" description="fmri_bids_recon/physio.py:81 -- _load_native_channel()'s label-parsing regex `_recording-([^_]+)_physio\.json$` fails on any compound (underscored) channel label. dcm2niix's real, confirmed output includes 'external_trigger' as a channel label, so this breaks the entire native physio ingestion path for every real physio-enabled run against ABCD/XA30 data (PhysioParseError is a GuardError subclass and is re-raised, halting the whole pipeline with exit 1, not just skipping one channel). Fix: change the regex to `_recording-(.+)_physio\.json$` (greedy .+ correctly captures compound labels since '_physio.json' is a fixed, single-occurrence anchor at the end of the filename). 12 tests in this session's suite (11 in test_physio.py, 1 in test_cli_integration.py) are pinned to the correct post-fix behavior and will pass once this lands; no test changes are needed alongside the fix." />
  </action_items>
</test_report>
