<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-08-19T17:34:18Z" />
  <pre_design_run>
    <total>594</total>
    <passed>503</passed>
    <failed>2</failed>
    <errors>0</errors>
    <coverage_pct />
    <failures>
      <failure test="test_a_config_error_exits_two" file="tests/test_cli_integration.py" line="235">
        <error_type>AssertionError</error_type>
        <message>assert 1 == 2 (exit code 1 returned instead of expected 2 for missing config file)</message>
        <traceback>FileNotFoundError: [Errno 2] No such file or directory: '...no_such_study.yaml'
tests/test_cli_integration.py:235: AssertionError: assert 1 == 2</traceback>
      </failure>
      <failure test="test_a_generic_pipeline_error_still_exits_two" file="tests/test_cli_integration.py" line="263">
        <error_type>AssertionError</error_type>
        <message>assert 1 == 2 (exit code 1 returned instead of expected 2 for generic pipeline error)</message>
        <traceback>CRITICAL fmri_bids_recon.__main__:__main__.py:173 Pipeline error: unexpected pipeline error
tests/test_cli_integration.py:263: AssertionError: assert 1 == 2</traceback>
      </failure>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
    <disposition test="test_a_generic_pipeline_error_still_exits_two" file="tests/test_cli_integration.py" classification="obsolete-test">
      <intended_contract>The BidsReconError catch-all handler in __main__.py deliberately changed exit code from 2 to 1 under the current session's approved harmonization change (catch-all exit codes must map to the invariant-violation code, not collide with ConfigError's exit 2).</intended_contract>
      <current_test_claim>The test's own comment states: "The fallback except BidsReconError handler remains exit 2 for pipeline errors that are not a version mismatch," and asserted _run_main(...) == 2.</current_test_claim>
      <evidence>fmri_bids_recon/__main__.py:172-174 (post-build state: logger.critical + sys.exit(1) for BidsReconError); harmonization brainstorm report bids-recon_brainstorm_20260819_165335.md topic T1, decision A2 (approved this session).</evidence>
      <action>re-express: assertion changed from == 2 to == 1; test renamed to test_a_generic_pipeline_error_still_exits_one; comment updated to describe the new exit-code contract.</action>
    </disposition>
    <disposition test="test_a_config_error_exits_two" file="tests/test_cli_integration.py" classification="product-bug">
      <intended_contract>__main__.py's own docstring (lines 123-130) documents exit code 2 as "Config / input validation error (stop, user must fix config)." A missing config file is definitionally a config/input error the user must fix.</intended_contract>
      <current_test_claim>assert _run_main(tmp_path / "no_such_study.yaml", monkeypatch) == 2, with comment "A nonexistent config file causes a config load error, exit code 2."</current_test_claim>
      <evidence>fmri_bids_recon/config.py:409-410, load_and_validate()'s docstring explicitly documents raising a bare FileNotFoundError (not ConfigError) when the config file does not exist. fmri_bids_recon/__main__.py's except chain (lines 160-177) has no specific FileNotFoundError handler, so it falls through to the bare Exception catch-all. Before this session's harmonization build, that catch-all coincidentally also exited 2, matching ConfigError's exit code by accident. The harmonization build (approved, session-scoped) correctly changed the bare-Exception catch-all to exit 1, which unmasks this pre-existing gap: a missing config file now exits 1 instead of the documented-intent exit 2. This defect predates and is independent of the harmonization changes; the harmonization build only exposed it by removing the coincidental shared exit code.</evidence>
      <action>route-to-implement: no test edit. Routed via action_items below.</action>
    </disposition>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>2</tests_created>
    <tests_modified>1</tests_modified>
    <files_created>
      <file path="tests/test_cli_integration.py" test_count="2" coverage_target="BIDS_VALIDATION_ERRORS graded_warning routing and status-field unification (both introduced by this session's harmonization build, previously untested)" />
    </files_created>
    <design_rationale>The pre-design run surfaced one obsolete-test (re-expressed per the approved exit-code contract change) and one product-bug (routed to implement, no test edit). Coverage-gap analysis of the harmonization build's second change (BIDS validation routed through graded_warning + status unification) found no existing test exercised BidsReconResult.warnings or .status directly; all existing coverage only checked __main__.py exit codes via _run_main, which cannot distinguish the new graded_warning routing mechanism from the prior BIDS-error-only status path (both produce the same exit code through the pre-existing exit-3 union check). Two tests were added, calling pipeline.run() directly to inspect the returned BidsReconResult: one confirms the BIDS_VALIDATION_ERRORS entry is appended to the warnings accumulator with high severity, and one confirms status="warning" fires from a non-BIDS high-severity warning (injected via monkeypatching pipeline_mod.classify), proving the status field is genuinely unified rather than still BIDS-error-specific.</design_rationale>
  </design_phase>
  <post_design_run>
    <total>596</total>
    <passed>506</passed>
    <failed>1</failed>
    <errors>0</errors>
    <coverage_pct />
    <failures>
      <failure test="test_a_config_error_exits_two" file="tests/test_cli_integration.py" line="235">
        <error_type>AssertionError</error_type>
        <message>assert 1 == 2 (missing config file exits 1 instead of the documented-intent exit 2)</message>
        <traceback>AssertionError: assert 1 == 2
tests/test_cli_integration.py:235: AssertionError</traceback>
        <likely_cause>Pre-existing product-bug (see disposition ledger above), left intentionally unmodified per the design-phase disposition. Not a regression from this session's changes.</likely_cause>
      </failure>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>1</bugs_routed_to_implement>
    <recommendation>implement_fixes</recommendation>
  </summary>
  <action_items>
    <item priority="P1" target_mode="implement" description="Missing-config-file path exits with code 1 instead of the documented exit-code-2 contract (config/input validation error, user must fix config). Fix by either (a) wrapping the FileNotFoundError raised in config.py's load_and_validate() as a ConfigError, or (b) adding a dedicated except FileNotFoundError handler in __main__.py ahead of the bare Exception catch-all, mapping to exit 2. Evidence and full disposition: see failing_test_dispositions above. Covered by tests/test_cli_integration.py::test_a_config_error_exits_two (unmodified, currently failing as an accurate defect report)." />
  </action_items>
</test_report>
