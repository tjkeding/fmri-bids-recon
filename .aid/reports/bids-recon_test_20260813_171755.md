<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-08-13T17:17:55Z" />
  <pre_design_run>
    <total>520</total>
    <passed>429</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>63</tests_created>
    <tests_modified>0</tests_modified>
    <files_created>
      <file path="tests/test_warnings.py" test_count="9" coverage_target="graded_warning(): return schema, severity constants, log-level selection (user_facing controls level, severity is a tag only), log-line format" />
      <file path="tests/test_tool_registry.py" test_count="31" coverage_target="preflight_tool_environments(): per-tool version parsing (dcm2niix/pydeface/flirt regexes), Class A strict/lenient and Class B comparison semantics, declarative enforcement (FSL mismatch never errors), conditional tool skipping when config.deface is False, FSLDIR-first binary resolution, ToolReport.has_errors aggregation, ToolVersionError escalation, the shipped tools.lock.yaml end to end" />
    </files_created>
    <files_extended>
      <file path="tests/test_config.py" test_count="9" coverage_target="load_and_validate() as the harmonized public API: parity with the load_config() wrapper, schema_version default and load-through, config_path provenance, subject-filter narrowing (valid subject, cross-product preservation, unknown subject raises ConfigError, no-filter processes all subjects)" />
      <file path="tests/test_cli_integration.py" test_count="8" coverage_target="orchestrator CLI contract: --config named form parity with positional, named-over-positional precedence, missing-config exit code 2, --subject filtering end to end (including an unconfigured subject exiting 2), --strict-versions threaded through to preflight_tool_environments (both true and false)" />
    </files_extended>
    <design_rationale>
      The prior build session (implement:build) harmonized bids-recon with the
      fmri-proc-orchestrator contract: a new graded_warning() framework, a
      Class A/B tool-version registry replacing the single-tool floor check,
      and a load_and_validate()/run() public API replacing the CLI-only
      entry point. None of these three additions had dedicated test
      coverage; the existing suite only exercised them indirectly through
      end-to-end CLI runs. This design phase adds direct unit coverage for
      each new module's documented contract (graded_warning's level-selection
      rule, the registry's four comparison branches, the config pipeline's
      new fields and subject-filter behavior) plus CLI-level coverage for
      the four new argument forms the orchestrator depends on
      (--config, --subject, --strict-versions, and the config_named-wins
      precedence rule). No prior test's assertion was touched: the pre-design
      run was already fully green (0 failures), so this cycle is purely
      additive.

      One test written during this design phase
      (test_the_subject_flag_filters_processing_to_that_subject) initially
      failed the post-design run. Root cause was a defect in the newly
      authored test's fixture setup, not in production code: the shared
      session fixture's convert_to_staging stub always returns a single
      hardcoded staging directory (staging_root/sub-001/ses-01), which
      coincides with pipeline.run()'s per-participant recomputed staging
      path only for the default single-subject case. For subject "002" the
      paths diverge, so the assemble phase silently found no intermediate
      JSON and skipped the participant. The test was corrected to stage
      dynamically per participant rather than weakening any assertion.
    </design_rationale>
  </design_phase>
  <post_design_run>
    <total>583</total>
    <passed>492</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items>
  </action_items>
</test_report>
