<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-07-30T18:33:55Z" />

  <pre_design_run>
    <total>0</total>
    <passed>0</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures></failures>
    <note>tests/simulated_bids/ did not exist before this invocation. This scope had no prior test suite of its own; the existing 432-test fmri_bids_recon suite under tests/ was left entirely out of scope for this invocation per the user's explicit instruction to keep the two suites separate and uncontaminated.</note>
  </pre_design_run>

  <failing_test_dispositions>
    <!-- No pre-existing failing tests to adjudicate: the design phase built new coverage from an empty directory rather than repairing an existing suite. -->
  </failing_test_dispositions>

  <design_phase>
    <tests_created>87</tests_created>
    <tests_modified>0</tests_modified>
    <files_created>
      <file path="tests/simulated_bids/conftest.py" test_count="0" coverage_target="Not a test file: registers the simulated_bids pytest marker and a default-skip hook so a plain `pytest tests/` invocation excludes this suite unless `-m simulated_bids` is given; also provides shared fixtures (rng, small_shape, small_voxel_size, synthetic_label_map). Deliberately independent from the root tests/conftest.py -- no shared file was touched." />
      <file path="tests/simulated_bids/__init__.py" test_count="0" coverage_target="Not a test file: makes tests/simulated_bids a proper package so pytest imports its conftest.py and test modules under a fully-qualified name (simulated_bids.*), avoiding a bare-module-name collision with the root tests/conftest.py that a same-named nested conftest.py would otherwise cause under pytest's default import mode." />
      <file path="tests/simulated_bids/test_tissue.py" test_count="25" coverage_target="tools/simulated_bids/tissue.py: label map I/O, rigid-body perturbation affines, nearest-neighbor resampling, per-tissue intensity lookup, B1/B0 field generation, B0 distortion, Rician noise statistics, and the volume/quick synthesis functions. Includes two permanent regression tests for the output-affine self-consistency defect and the header/content decoupling design found during the implement:build phase." />
      <file path="tests/simulated_bids/test_bold_signal.py" test_count="12" coverage_target="tools/simulated_bids/bold_signal.py: the double-gamma hemodynamic response function, condition-specific activation-map overlap allocation, functional-connectivity signal generation, and full 4D BOLD synthesis under both resting-state and task conditions. Includes regression tests for the HRF normalization defect (values previously reaching the billions) and the activation-overlap allocation defect (achieved overlap far exceeding configured targets), both found during the implement:build phase." />
      <file path="tests/simulated_bids/test_events.py" test_count="13" coverage_target="tools/simulated_bids/events.py: EN-back block timing, alternating load ordering, neural timecourse boxcar correctness, and BIDS events.tsv formatting. Includes a regression test for the condition-naming mismatch found during the build (an approximated naming scheme vs. the real ABCD protocol nomenclature)." />
      <file path="tests/simulated_bids/test_config_synthesis.py" test_count="22" coverage_target="The new tissue-synthesis constants in tools/simulated_bids/config.py: tissue-class/intensity-table consistency against the real 10-class BrainWeb model, physical sanity bounds on ADC and SNR values, AR(1) stationarity, and the perturbation-magnitude ordering (session greater than run greater than header) that keeps the header-only noise from ever disclosing the anatomical pose." />
      <file path="tests/simulated_bids/test_modalities_synthesis.py" test_count="7" coverage_target="The tissue-synthesis updates to tools/simulated_bids/modalities.py: generator functions using the passed-in session affine rather than the internal placeholder-affine helper, that helper's continued unchanged availability for the adversary module's own dependency, DWI b-value attenuation, and the fieldmap-pair generator's shared-anatomy-differing-only-in-distortion-polarity property." />
      <file path="tests/simulated_bids/test_adversaries_synthesis.py" test_count="8" coverage_target="The five migrated adversary functions (scan restart, stale series, extra localizer, fieldmap geometry mismatch, voxel-size drift) exercised against generated fixtures, plus a permanent regression guard against ever reintroducing the deleted noise-generator module." />
    </files_created>
    <design_rationale>
      The redesign work performed during the preceding implement:build session (BrainWeb tissue-based synthesis replacing procedural noise, across every modality and all migrated adversary functions) had no dedicated test coverage of its own; the only pre-existing test touching this code was tests/test_adversary_matrix.py, which checks the adversary assignment matrix's structural invariants, not the synthesis or mutation logic. Coverage was designed comprehensively across every new and changed module (tissue.py, bold_signal.py, events.py, the config.py additions, the modalities.py updates, and the adversaries.py migration), with particular weight on permanent regression tests for the four significant defects discovered and fixed during the build (the output-affine self-consistency bug, the HRF normalization bug, the activation-overlap allocation bug, and the EN-back condition-naming mismatch), so any future change reintroducing one of these defects is caught automatically. Tests use small synthetic shapes throughout to keep the suite fast, reserving real acquisition-resolution calls for a small number of tests that specifically need to exercise realistic volume sizes.
    </design_rationale>
  </design_phase>

  <post_design_run>
    <total>87</total>
    <passed>87</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures></failures>
  </post_design_run>

  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>

  <action_items>
    <item priority="P1" target_mode="implement" description="tools/simulated_bids/tissue.py and bold_signal.py introduced scipy.ndimage as a hard runtime dependency, which is not declared in the project's environment.yml. scipy was installed directly into the bids-recon conda environment (with explicit approval) so this test suite could run, but environment.yml itself was deliberately left unreconciled per the user's request to handle it separately, alongside the existing, unrelated numpy version drift already present between environment.yml (numpy==1.26.4) and the live environment (numpy 2.5.1)." />
    <item priority="P2" target_mode="implement" description="Regenerate the full adversarial and clean datasets (seventeen subjects, three sessions each) end to end. Only single-session and per-adversary fixture-level generation were exercised during the implement:build phase and this test phase; measured single-session generation time was approximately 196 seconds, consistent with the original estimate of two to four hours for the complete dataset." />
  </action_items>
</test_report>
