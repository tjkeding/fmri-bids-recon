<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-09-15T16:34:00Z" />
  <pre_design_run>
    <total>654</total>
    <passed>558</passed>
    <failed>1</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
      <failure test="test_a_two_run_tie_does_not_silently_promote_the_first_listed_count" file="tests/test_runs.py" line="194">
        <error_type>Failed</error_type>
        <message>DID NOT RAISE GuardError</message>
        <traceback>WARNING fmri_bids_recon.runs:warnings.py:44 [high:ABORTED_RUN_DETECTED] Task 'rest': 2 runs with different volume counts [87, 200]. Accepted the 200-volume run and excluded the other as a probable aborted acquisition. Manual review recommended.</traceback>
      </failure>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
    <disposition test="test_a_two_run_tie_does_not_silently_promote_the_first_listed_count" file="tests/test_runs.py" classification="obsolete-test">
      <intended_contract>Pre-T5: any tied modal volume count among unknown-task runs must raise GuardError (no principled exclusion target). Post-T5 (locked decision, this session's brainstorm; implemented as the runs.py tied-mode branch): a 2-run tie is presumed to be one complete acquisition and one aborted/restarted one, since fMRI protocols never over-acquire; the guard now accepts the longer run and excludes the shorter, reserving GuardError for n_runs greater than 2.</intended_contract>
      <current_test_claim>with pytest.raises(GuardError) as exc: check_volume_counts(...) — asserts exc.value.context["counts"] == [87, 200] and ["n_runs"] == 2.</current_test_claim>
      <evidence>bids-recon_brainstorm_20260915_120000.md topic T5, decision A1 (locked, approved); bids-recon_implement_plan_20260915_130000.md change C1; fmri_bids_recon/runs.py post-C1 code (git diff against HEAD shows the unconditional raise replaced by an n_runs==2/else split).</evidence>
      <action>Re-expressed. Renamed to test_a_two_run_tie_accepts_the_longer_run_as_a_probable_aborted_acquisition; strengthened to assert the full new contract (surviving run, excluded run with correct observed/expected volumes, registry entry, and the ABORTED_RUN_DETECTED warning's severity/user_facing/message fields) rather than merely "no exception raised." A new sibling test, test_a_three_run_tie_still_raises_guarderror, was added to close the now-uncovered n_runs greater than 2 GuardError path. tests/test_guard_coverage.py's also_raises_proof_test registry entry for exact_volume_counts was updated to point at the new sibling test, and its accompanying comment block was corrected to describe the n_runs greater than 2 boundary precisely; both are mechanical consequences of the rename, not new design decisions.</action>
    </disposition>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>8</tests_created>
    <tests_modified>1</tests_modified>
    <files_created>
      <file path="tests/test_runs.py" test_count="2" coverage_target="2-run tie now accepts the longer run (re-expressed); n_runs greater than 2 tie still raises GuardError (new)" />
      <file path="tests/conftest.py" test_count="0" coverage_target="Extends materialise()/stager() with an optional bval_values override (default unchanged) so tests can construct a .bval companion that exists but is all-zero" />
      <file path="tests/test_classify.py" test_count="2" coverage_target="_has_nonzero_bval guard routes Rule 5 and Rule 9 DWI_SBREF look-aheads to UNCLASSIFIED when the successor's .bval exists but is all-zero" />
      <file path="tests/test_assemble.py" test_count="3" coverage_target="Per-dir-entity DWI run-index grouping (same dir sequential; different dirs independent) and DWI_SBREF-to-parent-DWI pairing by temporal proximity" />
      <file path="tests/test_cli_integration.py" test_count="2" coverage_target="_ParticipantFormatter: omits context bracket by default, injects [sub-X ses-Y] when the pipeline ContextVars are active" />
      <file path="tests/test_guard_coverage.py" test_count="0" coverage_target="Mechanical: also_raises_proof_test pointer and comment updated to follow the test_runs.py rename" />
    </files_created>
    <design_rationale>Closes coverage gaps for all four implement changes from bids-recon_implement_plan_20260915_130000.md (the 2-run tie fix, the DWI_SBREF bval guard, the per-dir-entity DWI run-index, and the participant-context log formatter), and re-expresses the one pre-existing test whose assertion encoded the pre-T5 contract. Per Test Design Discipline, tests were written to the correct intended contract rather than to whatever the current implementation happens to do; two of the eight new/re-expressed tests fail as a direct result, surfacing two distinct product defects in the just-built code (see summary).</design_rationale>
  </design_phase>
  <post_design_run>
    <total>662</total>
    <passed>565</passed>
    <failed>2</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
      <failure test="test_a_two_run_tie_accepts_the_longer_run_as_a_probable_aborted_acquisition" file="tests/test_runs.py" line="203">
        <error_type>AssertionError</error_type>
        <message>assert [] == [13]</message>
        <traceback>WARNING fmri_bids_recon.runs:warnings.py:44 [high:ABORTED_RUN_DETECTED] Task 'rest': 2 runs with different volume counts [87, 200]. Accepted the 200-volume run and excluded the other as a probable aborted acquisition. Manual review recommended.</traceback>
        <likely_cause>Product bug in fmri_bids_recon/runs.py's just-built 2-run tied-mode branch (P0, blocks the fix for the original external-user crash). The accept/exclude loop that populates surviving_bolds/excluded_list/new_registry_entries (originally reachable unconditionally, because the pre-C1 tie branch always raised) remained nested inside the tie-check's else: only, at column 16 rather than being dedented to column 12 as a sibling of the whole if/else. Confirmed by direct reproduction: for counts [87, 200], check_volume_counts returns surviving=[], excluded=[], new_entries={} even though the emitted warning claims a run was accepted and the other excluded. Net effect: the crash is gone, but the task's runs now silently vanish from the BIDS tree with no run reaching either the output tree or the excluded/manual-review reporting path -- worse in one respect than the original crash, since there is no error at all. Fix: dedent the for series, tlabel in group: loop (and its body) in check_volume_counts so it runs after either branch of the tie-check, matching the tech spec's stated intent ("mode_count is now defined in both the n_runs==2 branch and the else branch").</likely_cause>
      </failure>
      <failure test="test_participant_formatter_omits_context_when_none_is_active" file="tests/test_cli_integration.py" line="577">
        <error_type>AssertionError</error_type>
        <message>assert 'INFO fmri_bi...]: no context' == 'INFO fmri_bi...t: no context'</message>
        <traceback>- INFO fmri_bids_recon.test: no context
+ INFO fmri_bids_recon.test [sub-001 ses-01]: no context</traceback>
        <likely_cause>Product bug in fmri_bids_recon/pipeline.py's just-built participant-context ContextVars (P1). Both the Phase 1 and Phase 3 per-participant loops call _current_sub.set(sub)/_current_ses.set(ses) unconditionally at the top of the loop body, but each loop also contains a pre-existing `continue` (Phase 1: `if should_skip(...): continue`; Phase 3: `if not json_path.exists(): continue`) that exits before reaching the reset() calls at the loop body's end. Any participant that takes either continue path leaks its sub/ses forward past the loop -- and, since ContextVar state is process-global, into every subsequent log message and every subsequent call to run() in the same process. Reproduced deterministically: running tests/test_cli_integration.py alone (35 tests, no other file) shows test_without_the_strict_versions_flag_preflight_receives_strict_false leaving _current_sub="001"/_current_ses="01" set (its participant hits the Phase 3 continue), which the very next test observes as unwanted context. Running the two new formatter tests in isolation, both pass. Fix: wrap the body of each per-participant loop (or at minimum the span from the set() calls through the reset() calls) in try/finally so the ContextVars are always cleared, regardless of which exit path the loop body takes.</likely_cause>
      </failure>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>2</bugs_routed_to_implement>
    <recommendation>implement_fixes</recommendation>
  </summary>
  <action_items>
    <item priority="P0" target_mode="implement" description="fmri_bids_recon/runs.py, check_volume_counts: the for series, tlabel in group: loop (and its body, populating surviving_bolds/excluded_list/new_registry_entries) is nested inside the tie-check's else: branch only. Dedent it to be a sibling of the whole if len(top) > 1 and top[0][1] == top[1][1]: / else: block, so it runs after both the n_runs==2 accept-longer-run branch and the no-tie branch. Without this fix, a 2-run tie's accepted run never reaches surviving_bolds and is silently dropped from the BIDS tree despite the emitted warning claiming it was accepted. Covered by tests/test_runs.py::test_a_two_run_tie_accepts_the_longer_run_as_a_probable_aborted_acquisition." />
    <item priority="P1" target_mode="implement" description="fmri_bids_recon/pipeline.py, run(): both the Phase 1 (CONVERT) and Phase 3 (ASSEMBLE) per-participant loops set _current_sub/_current_ses via ContextVar.set() unconditionally at the top of the loop body, but each loop's pre-existing continue path (Phase 1: should_skip; Phase 3: missing intermediate JSON) bypasses the reset() calls at the loop body's end, leaking the skipped participant's sub/ses into all subsequent log messages and subsequent run() invocations in the same process. Wrap each loop body (or the set-through-reset span) in try/finally so the ContextVars are always cleared. Covered by tests/test_cli_integration.py::test_participant_formatter_omits_context_when_none_is_active." />
  </action_items>
</test_report>
