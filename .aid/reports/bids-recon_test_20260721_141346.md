<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-07-21T14:13:46Z" />
  <scope>
    Validation round for the two most recent implementation changes to
    tools/simulated_bids/: the profile-driven CLI update to sandbox/smoke_run.py, and the
    bug fixes plus full 31/31 adversary-type coverage rewrite of sandbox/verify_dataset.py.
    Also implicitly covers the prior six-change build to tools/simulated_bids/ (config.py,
    scaffold.py, adversaries.py, manifest.py, __main__.py), since this is the first /test
    invocation since that build landed.
  </scope>
  <pre_design_run>
    <total>377</total>
    <passed>375</passed>
    <failed>0</failed>
    <errors>0</errors>
    <skipped>2</skipped>
    <duration_s>5.50</duration_s>
    <notes>
      Baseline run of the 18 pre-existing test files under tests/. All target bids_recon/
      (the main pipeline); none reference tools.simulated_bids, confirming zero pre-existing
      coverage of the generator package that the two most recent implementation rounds
      modified. The 2 skips are pre-existing, in test_guard_coverage.py (lines 153, 173),
      documenting that exact_volume_counts excludes the series rather than raising --
      intentional design documentation, not gaps requiring action.
    </notes>
  </pre_design_run>
  <failing_test_dispositions>
    None. The pre-design run produced 0 failed and 0 errors; the per-failure ledger required
    by the Test Design Discipline is empty by construction.
  </failing_test_dispositions>
  <design_phase>
    <files_created>
      <file path="tests/test_adversary_matrix.py" test_count="13" />
    </files_created>
    <rationale>
      Coverage gap analysis confirmed both implementation rounds landed exclusively in
      tools/simulated_bids/, which had zero existing test coverage. Scoped new coverage to
      the ADVERSARY_MATRIX configuration object rather than the generator's file-mutation
      logic: the matrix is a hand-authored, deterministic mapping where a single typo or
      duplicated ID would silently corrupt adversary coverage without raising, making it a
      high-value, low-cost target for structural invariant tests. The file-mutation logic
      (NIfTI generation, header mutation, directory scaffolding) is one-shot code intended
      for end-to-end validation against a real generated dataset via /run-local's validate
      submodule, per the user's explicit framing during this session's discussion.
    </rationale>
    <coverage>
      13 tests verifying: the matrix keys equal the adversarial roster exactly; the
      adversarial and clean rosters are disjoint; per-subject instance counts match the
      locked distribution [1,1,1,1,2,2,3,3,3,4,4,5,5]; total instance count is 35; all 31
      adversary types are covered; no subject has a duplicate adversary type; A15 is last in
      any subject's list (its duplicate-key defect is erased by later JSON round-trips); every
      matrix entry resolves to a callable apply_A{n} function; every entry declares at least
      one valid session; apply_adversaries' dispatch order echoes declaration order; the
      BOLD_REST_RUNS/BOLD_ENBACK_RUNS constants remain internally consistent with the A4 and
      A25 specs that depend on them; get_profile raises KeyError for unknown profiles and
      returns the correct dict for known ones.
    </coverage>
    <user_approval>Approved by user via the Pre-Write Approval Gate before any file was written.</user_approval>
  </design_phase>
  <post_design_run>
    <total>390</total>
    <passed>388</passed>
    <failed>0</failed>
    <errors>0</errors>
    <skipped>2</skipped>
    <duration_s>5.54</duration_s>
    <notes>
      377 pre-existing tests plus 13 new tests in tests/test_adversary_matrix.py, all
      passing. The 2 skips are the same pre-existing, documented skips from the baseline run.
      Independently confirmed via --collect-only: 390 tests collected. All 8 receipt
      freshness/consistency checks (nonce, clock window, duration, summary, collect_total,
      receipt file mtime, receipt file nonce, receipt file summary) verified as matching.
    </notes>
  </post_design_run>
  <summary>
    <all_passing>true</all_passing>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <net_new_coverage>13 tests added; 0 existing tests modified; 0 existing assertions touched.</net_new_coverage>
    <overall_assessment>
      Both implementation changes under test (the smoke_run.py profile CLI and the
      verify_dataset.py bug fixes and full-coverage rewrite) introduced no regressions in the
      existing 375-test bids_recon/ suite. The newly added structural invariant tests for
      ADVERSARY_MATRIX all pass against the current 13-subject, 35-instance, 31-type
      configuration, confirming the matrix itself is internally consistent (no duplicate
      assignments, no missing types, no unresolvable apply functions, A15 correctly
      positioned last where present, BOLD run-count constants consistent with A4/A25).
    </overall_assessment>
  </summary>
  <action_items>
    No items. This was a validation-only round with 0 product bugs found and 0 dispositions
    requiring routing.
  </action_items>
  <next_steps>
    Per Single-Pass Discipline, this /test invocation is complete after one full cycle. Per
    the user's stated sequencing, the next mode is /run-local: generate both the adversarial
    and clean profile datasets and validate them against the design specification. /run-local
    will require the ~/simulated-bids/ (or equivalent sandbox) filesystem grant, which must be
    surfaced for explicit user approval rather than self-granted.
  </next_steps>
</test_report>
