<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-07-28T13:08:00Z" />
  <pre_design_run>
    <total>434</total>
    <passed>432</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>13</tests_created>
    <tests_modified>2</tests_modified>
    <files_created>
      <file path="tests/test_deface.py" test_count="31" coverage_target="FSLDIR-based flirt resolution (_resolve_flirt), FSL environment preparation (_ensure_fsl_env), and the updated assert_deface_tools pre-flight check, all added in the deface.py FSL tool resolution change" />
    </files_created>
    <design_rationale>
      The implement pass (change C1) added three new code paths to deface.py that
      had zero direct test coverage: _resolve_flirt() (FSLDIR-first, PATH-fallback
      resolution), _ensure_fsl_env() (PATH amendment and FSLOUTPUTTYPE default
      applied before deface() shells out), and the FSLDIR-aware rewrite of
      assert_deface_tools(). The existing suite's three assert_deface_tools tests
      predated this change and only monkeypatched shutil.which, leaving them
      environment-dependent: a leaked FSLDIR pointing at a real flirt binary in
      the executing shell could make the "flirt absent" test pass for the wrong
      reason. Four tests cover _resolve_flirt() directly (FSLDIR hit, FSLDIR
      unset, FSLDIR set but bin/flirt absent, neither present). Five tests cover
      _ensure_fsl_env() (no-op when FSLDIR unset, PATH append, no duplicate
      append, FSLOUTPUTTYPE default, FSLOUTPUTTYPE preserved when already set).
      Two new assert_deface_tools tests prove FSLDIR resolution is actually
      consulted (not just PATH) and that the error message hints at FSLDIR when
      flirt is missing. One integration test confirms deface() itself invokes
      _ensure_fsl_env(). The two pre-existing flirt-related assert_deface_tools
      tests were re-expressed (not weakened) by adding
      monkeypatch.delenv("FSLDIR", raising=False) to close the environment-leakage
      gap; their assertions and pass/fail semantics are unchanged.
    </design_rationale>
  </design_phase>
  <post_design_run>
    <total>447</total>
    <passed>445</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items>
  </action_items>
</test_report>
