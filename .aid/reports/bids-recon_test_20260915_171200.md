<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-09-15T17:12:00Z" />
  <pre_design_run>
    <total>662</total>
    <passed>567</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures></failures>
  </pre_design_run>
  <failing_test_dispositions>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>2</tests_created>
    <tests_modified>0</tests_modified>
    <files_created>
      <file path="tests/test_cli_integration.py" test_count="2" coverage_target="pipeline.run()'s per-participant loop ContextVar cleanup: reset to default after normal completion, and reset even when a GuardError aborts the loop body mid-execution (the try/finally fix's core justification, previously untested since the pre-fix explicit reset() was unreachable on an exception path)" />
    </files_created>
    <design_rationale>Closes the residual coverage gap left after the runs.py accept/exclude loop dedent and the pipeline.py ContextVar try/finally fix were implemented. The runs.py fix required no new tests: the no-tie multi-run branch's loop position is unchanged by the dedent and was already covered by pre-existing tests, and the 2-run/3-run tie paths are covered by last session's re-expressed tests. The pipeline.py fix had a real gap: prior coverage only demonstrated the leak indirectly through test-order pollution, and never exercised the exception-abort path the try/finally structure specifically exists to guarantee. The two new tests exercise pipeline.run() directly rather than the formatter in isolation, with the guard-violation test reusing the existing two-same-direction-fieldmaps setup that is known to raise a GuardError from group_fieldmaps during the Phase 1 loop body. A third possible test, direct multi-participant coverage of the two continue paths (Phase 1 should_skip, Phase 3 missing intermediate JSON), was surfaced to the user and explicitly deferred: it would require extending the session fixture to stage a second participant, and Python's try/finally semantics already guarantee correctness there independent of which exit path is taken.</design_rationale>
  </design_phase>
  <post_design_run>
    <total>664</total>
    <passed>569</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures></failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items>
  </action_items>
</test_report>
