<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-08-18T16:58:20Z" />

  <pre_design_run>
    <total>574</total>
    <passed>0</passed>
    <failed>0</failed>
    <errors>1</errors>
    <coverage_pct></coverage_pct>
    <failures>
      <failure test="ERROR collecting tests/test_warnings.py" file="tests/test_warnings.py" line="18">
        <error_type>ImportError</error_type>
        <message>cannot import name 'SEVERITY_CRITICAL' from 'fmri_bids_recon.warnings'</message>
        <traceback>tests/test_warnings.py:18: in &lt;module&gt;
    from fmri_bids_recon.warnings import (
E   ImportError: cannot import name 'SEVERITY_CRITICAL' from 'fmri_bids_recon.warnings'</traceback>
      </failure>
    </failures>
  </pre_design_run>

  <failing_test_dispositions>
    <disposition test="tests/test_warnings.py (module-level collection failure)" file="tests/test_warnings.py" classification="obsolete-test">
      <intended_contract>graded_warning() has three independently testable parts (return schema, level-selection rule, log-line format), all documented as unchanged by the harmonization's severity-vocabulary change.</intended_contract>
      <current_test_claim>Imports SEVERITY_WARN="warn" and SEVERITY_CRITICAL="strong" from fmri_bids_recon.warnings.</current_test_claim>
      <evidence>fmri_bids_recon/warnings.py now exports only SEVERITY_LOW/SEVERITY_MEDIUM/SEVERITY_HIGH ("low"/"medium"/"high"); implement_plan C3 states graded_warning()'s signature and behavior are unchanged.</evidence>
      <action>re-express: swap SEVERITY_WARN to SEVERITY_LOW and SEVERITY_CRITICAL to SEVERITY_HIGH throughout, add a constant-value assertion for SEVERITY_MEDIUM. No behavioral assertion changed.</action>
    </disposition>

    <disposition test="tests/test_tool_registry.py::lockfile fixture (and 11 dependent tests) plus test_load_lockfile_parses_the_project_lockfile" file="tests/test_tool_registry.py" classification="obsolete-test">
      <intended_contract>preflight_tool_environments() and _load_lockfile() read the lockfile's top-level binaries dict under the key "binaries", per the cross-module schema rename.</intended_contract>
      <current_test_claim>The lockfile fixture writes {"schema_version": ..., "tools": tools}; the direct-parse test asserts data["schema_version"] and "dcm2niix" in data["tools"].</current_test_claim>
      <evidence>tool_registry.py:167 reads lock_data.get("binaries", {}); the real tools.lock.yaml uses top-level keys lockfile_version/binaries (implement_plan C1).</evidence>
      <action>re-express: fixture writes {"lockfile_version": "1.0.0", "binaries": tools}; direct-parse test asserts data["lockfile_version"] and "dcm2niix" in data["binaries"]. No behavioral assertion changed.</action>
    </disposition>

    <disposition test="tests/test_assemble.py::test_inconsistent_patient_ids_raise_a_warning_that_carries_no_identifier" file="tests/test_assemble.py" classification="obsolete-test">
      <intended_contract>A PatientID mismatch across series within one session is a possible identity mix-up and must halt the session (GuardError), per the user-approved H2c halt-elevation decision.</intended_contract>
      <current_test_claim>Calls assemble() expecting a normal return with result.patient_id_warnings populated with a PHI-safe message.</current_test_claim>
      <evidence>stage4_assemble.py:533-549 now emits a SEVERITY_HIGH graded_warning and then raises GuardError (context guard="patient_id_unique"); AssemblyResult.patient_id_warnings is hardcoded to [] at every return.</evidence>
      <action>re-express (renamed to test_inconsistent_patient_ids_halt_assembly_without_leaking_an_identifier): wrap the call in pytest.raises(GuardError), assert context["guard"]=="patient_id_unique" and context["n_patient_ids"]==2, and strengthen the PHI-safety check to assert neither raw PatientID value appears in str(exc.value) (previously checked only the returned warning string).</action>
    </disposition>

    <disposition test="tests/test_runs.py::test_a_two_run_tie_does_not_silently_promote_the_first_listed_count" file="tests/test_runs.py" classification="obsolete-test">
      <intended_contract>An undecidable modal-count tie among within-session BOLD runs has no principled exclusion target and must halt the session (GuardError), per the user-approved H2/C5 halt-elevation decision, adjudicated against the test_guard_coverage.py conflict below.</intended_contract>
      <current_test_claim>Expects check_volume_counts() to return normally, with the 200-volume run surviving and the tie not silently promoting either count via soft warning + retain-all.</current_test_claim>
      <evidence>runs.py:122-133 now raises GuardError (context guard="exact_volume_counts", counts, n_runs) for this branch instead of appending a graded_warning and retaining both series.</evidence>
      <action>re-express: assert pytest.raises(GuardError) with context counts==[87,200], n_runs==2, task_label=="rest". Strengthens the original intent (never silently promote a tied count) into a hard-failure guarantee instead of a soft one.</action>
    </disposition>

    <disposition test="tests/test_cli_integration.py::test_a_tool_version_mismatch_exits_two" file="tests/test_cli_integration.py" classification="obsolete-test">
      <intended_contract>A genuine tool version mismatch (ToolVersionError) is an environment issue and must exit 4, distinct from a generic pipeline/config error (exit 2).</intended_contract>
      <current_test_claim>Simulates the scenario by raising a generic BidsReconError (not the real ToolVersionError the test's name references) and asserts exit code 2.</current_test_claim>
      <evidence>__main__.py now has a dedicated `except ToolVersionError` handler ahead of the generic `except BidsReconError` handler, mapping to exit 4; implement_plan's newly_observed_issue documents this was a pre-existing miscatching bug, user-approved for fix in C7.</evidence>
      <action>re-express (renamed to test_a_tool_version_mismatch_exits_four): raise the real ToolVersionError, assert exit code 4. A new test (test_a_generic_pipeline_error_still_exits_two) was added to preserve the generic-BidsReconError-exits-2 coverage this rename would otherwise have dropped.</action>
    </disposition>
  </failing_test_dispositions>

  <architectural_conflict_resolved>
    <finding>tests/test_guard_coverage.py registered exact_volume_counts with error=None and asserted, via three tests, that it is "the only guard that excludes rather than halts." The harmonization's C5 change makes the modal-count-tie branch of check_volume_counts() raise GuardError directly, contradicting that invariant. The implement plan's architectural note checked only ALL_GUARD_NAMES/meta-guard consistency and did not address this conflict.</finding>
    <disposition>ambiguous - halted the design phase and surfaced both options to the user with the supporting evidence, per the Test Design Discipline's prohibition on silently resolving a live conflict between two authoritative, user-approved artifacts.</disposition>
    <user_decision>Keep the halt; register exact_volume_counts as a dual-mode guard in test_guard_coverage.py rather than reverting C5.</user_decision>
    <resolution>Extended the Guard dataclass with also_raises/also_raises_proof_module/also_raises_proof_test fields. exact_volume_counts now carries error=None (primary mode: exclude) plus also_raises=GuardError (secondary mode: halt on an undecidable tie), pointing at the re-expressed tie test as its proof. Updated the three parametrized tests that previously skipped unconditionally on error is None to check also_raises before skipping. Added two new tests: every guard has at least one raise path (closes the skip loophole a future guard with neither error nor also_raises would exploit), and exact_volume_counts remains the only guard with a secondary halt path. The original "only guard that excludes" assertion (based on guard.error is None) is preserved verbatim and still passes, since exact_volume_counts' PRIMARY resolution is unchanged.</resolution>
  </architectural_conflict_resolved>

  <design_phase>
    <tests_created>10</tests_created>
    <tests_modified>7</tests_modified>
    <files_created>
      <file path="tests/test_tool_registry.py" test_count="4" coverage_target="Previously-uncovered Python version floor check in preflight_tool_environments(): at/above floor ok, below-floor warning (lenient), below-floor error + ToolVersionError escalation (strict), and no-pin-present skip." />
      <file path="tests/test_assemble.py" test_count="1" coverage_target="Previously-uncovered fieldmap_pair_complete GuardError: a fieldmap classified with a role but absent from mapping.pairs (a wiring defect distinct from the existing odd-count PhaseEncodingError halt)." />
      <file path="tests/test_guard_coverage.py" test_count="2" coverage_target="Closes the skip loophole in the engine-raises/violation-is-blocking parametrized checks; pins the dual-mode guard as exceptional (exactly one guard, not silently spreading)." />
      <file path="tests/test_cli_integration.py" test_count="3" coverage_target="Generic BidsReconError still exits 2 (coverage preserved after the version-mismatch test was repurposed); exit-code-3 union via a high-severity warning with no BIDS validation errors; BidsReconResult.output_dir field, previously untested anywhere in the suite." />
    </files_created>
    <design_rationale>
      Coverage additions target every behavioral seam the harmonization build introduced that the pre-existing suite did not already exercise: the runtime Python floor check (C2), the assemble()-level fieldmap halt distinct from the pairing-level halt (C6), the guard-coverage roster's now-dual-mode invariant (C5, post-adjudication), and the exit-code-3 union plus the new output_dir field on BidsReconResult (C7/C8). Every re-expression (see failing_test_dispositions) preserves or strengthens its prior postcondition; none weaken an assertion, and none were resolved by editing away a failure without evidence of a genuine contract change.
    </design_rationale>
  </design_phase>

  <post_design_run>
    <total>594</total>
    <passed>505</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures></failures>
  </post_design_run>

  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>

  <action_items>
  </action_items>

  <notes>
    The post-design run's 89 skips are the pre-existing tests/simulated_bids/ suite, gated by a "simulated_bids tests are excluded by default; run with -m simulated_bids to include them" marker unrelated to this harmonization round; confirmed via -rs categorization before writing this report. Both the pre-design and post-design runs were dispatched to execution-agent-sonnet-medium and receipt-verified (nonce, clock window, duration, summary-line, and tee'd-file cross-checks all matched) per the Agent Return Validation Gate Stage 3 doctrine.
  </notes>
</test_report>
