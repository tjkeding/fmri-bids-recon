<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-07-24T16:05:00-04:00" />
  <pre_design_run>
    <total>423</total>
    <passed>421</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>11</tests_created>
    <tests_modified>0</tests_modified>
    <files_created>
      <file path="tests/test_sanitize_sys_path.py" test_count="11" coverage_target="_sanitize_sys_path() in fmri_bids_recon/__main__.py: sys.path stripping of foreign-version entries, _STRIPPED_PATHS accumulation, and PYTHONPATH partial/full cleanup semantics" />
    </files_created>
    <design_rationale>The build phase added _sanitize_sys_path() to strip foreign-version site-packages entries from sys.path and PYTHONPATH before scientific library imports, closing a real crash observed on the collaborator's HPC cluster (Python 3.14 conda env importing numpy C extensions compiled for FSL's bundled Python 3.11 after `module load FSL`). No test coverage existed for this new function. Eleven tests were designed to lock the following behaviors as explicit contracts: a clean sys.path is a true no-op; current-version and non-versioned entries are always preserved; foreign-version entries (single and multiple) are stripped and recorded in the module-level _STRIPPED_PATHS list, which accumulates rather than resets across calls; PYTHONPATH is only inspected when sys.path itself contains a foreign entry (an intentional asymmetry in the implementation, tested explicitly so it is never mistaken for a bug); PYTHONPATH cleanup correctly handles partial retention, full deletion when every entry is foreign, absence of the variable entirely, and non-versioned entries mixed in.</design_rationale>
  </design_phase>
  <post_design_run>
    <total>434</total>
    <passed>432</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items>
  </action_items>
</test_report>
