<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-09-17T18:43:19+00:00" />
  <pre_design_run>
    <total>711</total>
    <passed>616</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </pre_design_run>
  <failing_test_dispositions>
  </failing_test_dispositions>
  <design_phase>
    <tests_created>47</tests_created>
    <tests_modified>0</tests_modified>
    <files_created>
      <file path="tests/test_group_report.py" test_count="43" coverage_target="parse_conversion_report() field extraction across all 7 report sections; FOLLOWUP_INSTRUCTIONS completeness (codebase-scanning, bidirectional: no missing, no orphaned entries); write_group_summary() aggregation across subjects (overview totals, high vs. medium/low flag routing, excluded/unclassified/auto-registered tables, provenance, timestamped non-overwriting filenames, multi-session ordering); _pad_table and _parse_table_rows helper primitives; a section-boundary regression guard preventing Section 6 fieldmap bullets from leaking into Section 7 PatientID parsing." />
    </files_created>
    <design_rationale>
      Two target files were needed, matching the codebase's existing separation between per-module tests and entry-point wiring tests. tests/test_group_report.py provides direct coverage for the new fmri_bids_recon/group_report.py module, mirroring the fixture pattern already established in tests/test_report.py (writing real per-subject reports via the real write_conversion_report() writer, then feeding them through the new parser, rather than hand-typed Markdown that could drift from the actual format). tests/test_cli_integration.py already contains an AST-based mechanism (STAGE_FUNCTIONS) that statically verifies every stage function pipeline.py calls binds against a real signature; write_group_summary is a new such call site introduced by this cycle's build and was added to that mechanism, plus three new end-to-end tests covering the Phase 8 non-blocking failure contract, file production during a real run, and correct provenance threading.

      The completeness test for FOLLOWUP_INSTRUCTIONS was built as a codebase scanner (regex over graded_warning() call sites across the package) rather than a hand-maintained list of expected codes, so it tracks the codebase directly: a future graded_warning() call site added without a matching FOLLOWUP_INSTRUCTIONS entry fails immediately, and an entry for a code no longer emitted anywhere is caught as an orphan. This scanner was validated against the real codebase before being committed to the test file: 23 (severity, code) pairs found across 8 source modules, exactly matching the 23 entries in FOLLOWUP_INSTRUCTIONS, zero missing, zero orphaned.
    </design_rationale>
  </design_phase>
  <post_design_run>
    <total>758</total>
    <passed>663</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct></coverage_pct>
    <failures>
    </failures>
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items>
    <item priority="P2" target_mode="implement" description="write_group_summary() sorts subjects lexicographically by the string (sub, ses) tuple. This is correct for the current single-digit test subjects and correctly orders multiple sessions within one subject, but if a study exceeds 9 subjects with unpadded numeric IDs (e.g., sub-10 sorting before sub-2), the Run Overview table's subject ordering would not match numeric expectations. This was an accepted design choice in the approved tech spec, not a deviation, and no test failure resulted; it is surfaced here as a design observation for a longitudinal study whose subject count is expected to grow over time (per the group-summary brainstorm's stated intent), not as a defect found during testing." />
  </action_items>
</test_report>
