<test_report>
  <meta project="bids-recon" mode="test" timestamp="2026-08-19T18:22:00Z" />
  <pre_design_run>
    <total>596</total>
    <passed>507</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct />
    <failures />
  </pre_design_run>
  <failing_test_dispositions />
  <design_phase>
    <tests_created>1</tests_created>
    <tests_modified>0</tests_modified>
    <files_created>
      <file path="tests/test_config.py" test_count="1" coverage_target="Unit-level verification that load_and_validate() raises ConfigError (not the underlying FileNotFoundError) for a missing config file, closing the gap where only the CLI-level exit-code test exercised this path" />
    </files_created>
    <design_rationale>The pre-design run showed zero failures: the fix routing a missing config file's FileNotFoundError through ConfigError landed cleanly and the previously-failing test_a_config_error_exits_two now passes. Coverage-gap analysis found that this new exception-typing behavior in load_and_validate() had no direct unit test; only the CLI-level integration test (test_a_config_error_exits_two) exercised it, and that test checks the exit code, not the exception type or message. This mirrors an existing convention in test_config.py (e.g., test_all_dicom_paths_missing_raises_config_error), which tests its corresponding ConfigError directly via pytest.raises. One test was added asserting load_and_validate() raises ConfigError with the missing path embedded in the message, pinning the contract at the unit layer where the fix was actually made.</design_rationale>
  </design_phase>
  <post_design_run>
    <total>597</total>
    <passed>508</passed>
    <failed>0</failed>
    <errors>0</errors>
    <coverage_pct />
    <failures />
  </post_design_run>
  <summary>
    <assertions_preserved_or_strengthened>true</assertions_preserved_or_strengthened>
    <bugs_routed_to_implement>0</bugs_routed_to_implement>
    <recommendation>proceed_to_document</recommendation>
  </summary>
  <action_items />
</test_report>
